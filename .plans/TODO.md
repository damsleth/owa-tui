# TODO

- [ ] Commit fixture-mode seam + tui-test e2e suite (src/owa_tui/fixtures.py, e2e/actions.test.ts, e2e/fixtures/) and the 3 bug fixes: graph menu crash, cal respond status persistence, cal declined/tentative verb typo
- [ ] Add optional live-auth e2e smoke run against real Graph (deferred — flaky/CI-hostile; gate behind an env flag, separate from fixture-mode suite)
